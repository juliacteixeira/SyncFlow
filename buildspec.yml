version: 0.2

phases:
  build:
    commands:
      - n 18.18.2
      - node -v
      - npm -v
      - npm install -g @angular/cli
      - cd frontend
      - npm install
      - ng build --prod
      - cd ../backend
      - npm install
