version: 0.2

phases:
  build:
    commands:
      - node -v
      - npm -v
      - npm install -g n
      - node -v
      - npm -v
      - npm install -g @angular/cli
      - cd frontend
      - npm install
      - ng build --prod
      - cd ../backend
      - npm install
